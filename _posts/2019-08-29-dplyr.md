---
title: 'DPLYR Blocks'
type: 'DPLYR'
layout: nil
---


<img src="/guide/images/dplyr_filter.png" width="30%">
# Filter

### Description:
Use the Filter block to find rows/cases where conditions are true.

### Arguments:
Logical predicates defined in terms of the variables within the data block. Multiple conditions are combined with AND blocks. Only rows where the condition evaluates to TRUE are kept.

### Examples:
<br>
<img src="/guide/images/dplyr_filter_example.png" width="50%">
<br>
<img src="/guide/images/dplyr_filter_example_multiple.png" width="90%">

<img src="/guide/images/dplyr_groupBy.png" width="30%">

### Description:

Most data operations are done on groups defined by variables. The group by block takes an existing table and converts it into a grouped table where operations are performed "by group". The ungroup() block can be used to remove grouping.

### Arguments: 
The name of the columns to group by. 

### Examples:
<br>
<img src="/guide/images/dplyr_groupBy_example.png" width="50%">
<br>
<img src="/guide/images/dplyr_groupBy_example_multiple.png" width="90%">

# Mutate
# Reverse
# Select
# Sort
# Summarize
# Ungroup