---
title: 'DPLYR Blocks'
type: 'DPLYR'
layout: nil
---

<h1><a id="#filter">Filter</a></h1>
<div id="wrapper">
    <div id="first"> 
    <img src="/guide/images/dplyr_filter.png" width="100%"> 
    </div>
    <div id="second">
    <h3>Description:</h3>
    <p>Use the Filter block to find rows/cases where conditions are true.</p>
    <h3>Arguments</h3>
    <p>Logical predicates defined in terms of the variables within the data block. Multiple conditions are combined with AND blocks. Only rows where the condition evaluates to TRUE are kept.</p>
    <h3>Examples:</h3>
    <img src="/guide/images/dplyr_filter_example.png" width="80%"><br>
    <img src="/guide/images/dplyr_filter_example_multiple.png" width="80%">
    </div>
</div>

# Group By
<div id="wrapper">
    <div id="first"> 
    <img src="/guide/images/dplyr_groupBy.png" width="100%"> 
    </div>
    <div id="second">
    <h3>Description:</h3>
    <p>Most data operations are done on groups defined by variables. The group by block takes an existing table and converts it into a grouped table where operations are performed "by group". The ungroup() block can be used to remove grouping.</p>
    <h3>Arguments</h3>
    <p>The name of the column(s) to group by. </p>
    <h3>Examples:</h3>
    <img src="/guide/images/dplyr_groupBy_example.png" width="80%"><br>
    <img src="/guide/images/dplyr_groupBy_example_multiple.png" width="80%">
    </div>
</div>

# Mutate
<div id="wrapper">
    <div id="first"> 
    <img src="/guide/images/dplyr_mutate.png" width="100%"> 
    </div>
    <div id="second">
    <h3>Description:</h3>
    <p>Most data operations are done on groups defined by variables. The group by block takes an existing table and converts it into a grouped table where operations are performed "by group". The ungroup() block can be used to remove grouping.</p>
    <h3>Arguments</h3>
    <p>The name of the column(s) to group by. </p>
    <h3>Examples:</h3>
    <img src="/guide/images/dplyr_groupBy_example.png" width="80%"><br>
    <img src="/guide/images/dplyr_groupBy_example_multiple.png" width="80%">
    </div>
</div>

# Reverse
<div id="wrapper">
    <div id="first"> 
    <img src="/guide/images/dplyr_reverse.png" width="100%"> 
    </div>
    <div id="second">
    <h3>Description:</h3>
    <p>Most data operations are done on groups defined by variables. The group by block takes an existing table and converts it into a grouped table where operations are performed "by group". The ungroup() block can be used to remove grouping.</p>
    <h3>Arguments</h3>
    <p>The name of the column(s) to group by. </p>
    <h3>Examples:</h3>
    <img src="/guide/images/dplyr_groupBy_example.png" width="80%"><br>
    <img src="/guide/images/dplyr_groupBy_example_multiple.png" width="80%">
    </div>
</div>

# Select
<div id="wrapper">
    <div id="first"> 
    <img src="/guide/images/dplyr_select.png" width="100%"> 
    </div>
    <div id="second">
    <h3>Description:</h3>
    <p>Most data operations are done on groups defined by variables. The group by block takes an existing table and converts it into a grouped table where operations are performed "by group". The ungroup() block can be used to remove grouping.</p>
    <h3>Arguments</h3>
    <p>The name of the column(s) to group by. </p>
    <h3>Examples:</h3>
    <img src="/guide/images/dplyr_groupBy_example.png" width="80%"><br>
    <img src="/guide/images/dplyr_groupBy_example_multiple.png" width="80%">
    </div>
</div>

# Sort
<div id="wrapper">
    <div id="first"> 
    <img src="/guide/images/dplyr_sort.png" width="100%"> 
    </div>
    <div id="second">
    <h3>Description:</h3>
    <p>Most data operations are done on groups defined by variables. The group by block takes an existing table and converts it into a grouped table where operations are performed "by group". The ungroup() block can be used to remove grouping.</p>
    <h3>Arguments</h3>
    <p>The name of the column(s) to group by. </p>
    <h3>Examples:</h3>
    <img src="/guide/images/dplyr_groupBy_example.png" width="80%"><br>
    <img src="/guide/images/dplyr_groupBy_example_multiple.png" width="80%">
    </div>
</div>

# Summarize
<div id="wrapper">
    <div id="first"> 
    <img src="/guide/images/dplyr_summarize.png" width="100%"> 
    </div>
    <div id="second">
    <h3>Description:</h3>
    <p>Most data operations are done on groups defined by variables. The group by block takes an existing table and converts it into a grouped table where operations are performed "by group". The ungroup() block can be used to remove grouping.</p>
    <h3>Arguments</h3>
    <p>The name of the column(s) to group by. </p>
    <h3>Examples:</h3>
    <img src="/guide/images/dplyr_groupBy_example.png" width="80%"><br>
    <img src="/guide/images/dplyr_groupBy_example_multiple.png" width="80%">
    </div>
</div>

# Ungroup
<div id="wrapper">
    <div id="first"> 
    <img src="/guide/images/dplyr_ungroup.png" width="100%"> 
    </div>
    <div id="second">
    <h3>Description:</h3>
    <p>Most data operations are done on groups defined by variables. The group by block takes an existing table and converts it into a grouped table where operations are performed "by group". The ungroup() block can be used to remove grouping.</p>
    <h3>Arguments</h3>
    <p>The name of the column(s) to group by. </p>
    <h3>Examples:</h3>
    <img src="/guide/images/dplyr_groupBy_example.png" width="80%"><br>
    <img src="/guide/images/dplyr_groupBy_example_multiple.png" width="80%">
    </div>
</div>